<style>
  .manage-container {
    max-width: 1000px;
    margin: 2rem auto;
    padding: 0 1rem;
  }
  
  .manage-header {
    margin-bottom: 2rem;
    padding-bottom: 1rem;
    border-bottom: 2px solid #ddd;
  }
  
  .manage-header h1 {
    font-size: 2rem;
    margin-bottom: 0.5rem;
  }
  
  .tag-info {
    display: flex;
    gap: 1rem;
    margin-bottom: 1rem;
    color: #666;
  }
  
  .alert {
    padding: 1rem;
    margin-bottom: 1rem;
    border-radius: 4px;
  }
  
  .alert-success {
    background-color: #d4edda;
    color: #155724;
    border: 1px solid #c3e6cb;
  }
  
  .section {
    margin-bottom: 3rem;
  }
  
  .section-title {
    font-size: 1.5rem;
    margin-bottom: 1rem;
    color: #333;
  }
  
  .requests-list {
    background: white;
    border: 1px solid #ddd;
    border-radius: 8px;
    overflow: hidden;
  }
  
  .request-item {
    padding: 1.5rem;
    border-bottom: 1px solid #eee;
  }
  
  .request-item:last-child {
    border-bottom: none;
  }
  
  .request-header {
    display: flex;
    justify-content: space-between;
    align-items: start;
    margin-bottom: 0.5rem;
  }
  
  .request-user {
    font-weight: bold;
    font-size: 1.1rem;
  }
  
  .request-date {
    color: #666;
    font-size: 0.875rem;
  }
  
  .request-message {
    background-color: #f8f9fa;
    padding: 1rem;
    border-radius: 4px;
    margin: 1rem 0;
    font-style: italic;
  }
  
  .request-actions {
    display: flex;
    gap: 1rem;
    align-items: flex-end;
  }
  
  .response-form {
    flex: 1;
  }
  
  .form-group {
    margin-bottom: 0.5rem;
  }
  
  .form-group label {
    display: block;
    margin-bottom: 0.25rem;
    font-weight: 500;
    font-size: 0.875rem;
  }
  
  .form-group input[type="text"] {
    width: 100%;
    padding: 0.5rem;
    border: 1px solid #ddd;
    border-radius: 4px;
  }
  
  .action-buttons {
    display: flex;
    gap: 0.5rem;
  }
  
  .btn {
    padding: 0.5rem 1rem;
    border: none;
    border-radius: 4px;
    cursor: pointer;
    font-size: 0.875rem;
    transition: opacity 0.2s;
  }
  
  .btn:hover {
    opacity: 0.9;
  }
  
  .btn-success {
    background-color: #28a745;
    color: white;
  }
  
  .btn-danger {
    background-color: #dc3545;
    color: white;
  }
  
  .btn-secondary {
    background-color: #6c757d;
    color: white;
  }
  
  .empty-state {
    text-align: center;
    padding: 3rem;
    color: #666;
  }
  
  .permissions-grid {
    display: grid;
    grid-template-columns: repeat(auto-fill, minmax(250px, 1fr));
    gap: 1rem;
  }
  
  .permission-card {
    background: white;
    border: 1px solid #ddd;
    border-radius: 8px;
    padding: 1rem;
  }
  
  .permission-user {
    font-weight: bold;
    margin-bottom: 0.25rem;
  }
  
  .permission-type {
    display: inline-block;
    padding: 0.25rem 0.5rem;
    background-color: #007bff;
    color: white;
    border-radius: 4px;
    font-size: 0.75rem;
    margin-top: 0.5rem;
  }
  
  .permission-universal {
    background-color: #28a745;
  }
  
  .owners-list {
    display: grid;
    grid-template-columns: repeat(auto-fill, minmax(250px, 1fr));
    gap: 1rem;
  }
  
  .owner-card {
    background: white;
    border: 1px solid #ddd;
    border-radius: 8px;
    padding: 1rem;
  }
  
  .owner-name {
    font-weight: bold;
    margin-bottom: 0.25rem;
  }
  
  .owner-type {
    display: inline-block;
    padding: 0.25rem 0.5rem;
    background-color: #ffc107;
    color: #333;
    border-radius: 4px;
    font-size: 0.75rem;
  }
  
  .owner-type.founder {
    background-color: #ffd700;
  }
  
  .back-link {
    display: inline-block;
    margin-bottom: 1rem;
    color: #007bff;
    text-decoration: none;
  }
  
  .back-link:hover {
    text-decoration: underline;
  }
</style>

<div class="manage-container">
  <a href="/tags" class="back-link">← Back to Tags</a>
  
  <div class="manage-header">
    <h1>Manage Tag: #<%= tag.name %></h1>
    <div class="tag-info">
      <span>📝 <%= tag.short_description || tag.description || 'No description' %></span>
      <span>|</span>
      <span><%= tag.is_public ? '🌍 Public' : '🔒 Private' %></span>
      <span>|</span>
      <span>📊 Usage: <%= tag.usage_count || 0 %></span>
    </div>
    
    <% if (pendingRequests && pendingRequests.length > 0) { %>
      <div class="pending-notification">
        <strong>📬 You have <%= pendingRequests.length %> pending access request<%= pendingRequests.length === 1 ? '' : 's' %></strong>
        - scroll down to review
      </div>
    <% } %>
    <% 
      // Check if current user is the founder
      const isFounder = owners && owners.some(o => o.user_id === user.id && o.ownership_type === 'founder');
    %>
    <% if (isFounder) { %>
      <button class="btn btn-primary" style="margin-top: 1rem;" onclick="showEditTagModal()">
        ✏️ Edit Tag Details
      </button>
      <p style="color: #666; font-size: 0.875rem; margin-top: 0.5rem;">
        As the founder, only you can edit this tag's details.
      </p>
    <% } else { %>
      <p style="color: #666; font-size: 0.875rem; margin-top: 0.5rem;">
        Only the tag founder can edit tag details. Co-owners can manage access requests.
      </p>
    <% } %>
  </div>
  
  <% if (message) { %>
    <div class="alert alert-success"><%= message %></div>
  <% } %>
  
  <!-- Pending Access Requests -->
  <div class="section">
    <h2 class="section-title">
      📋 Pending Access Requests 
      <% if (pendingRequests && pendingRequests.length > 0) { %>
        <span class="pending-badge">
          <%= pendingRequests.length %>
        </span>
      <% } %>
    </h2>
    
    <% if (pendingRequests && pendingRequests.length > 0) { %>
      <div class="requests-list highlight-border">
        <% pendingRequests.forEach(request => { %>
          <div class="request-item">
            <div class="request-header">
              <div>
                <div class="request-user">@<%= request.handle %></div>
                <div class="request-date">Requested <%= new Date(request.requested_at).toLocaleDateString() %></div>
              </div>
            </div>
            
            <% if (request.request_message) { %>
              <div class="request-message">
                "<%= request.request_message %>"
              </div>
            <% } %>
            
            <% if (request.bio) { %>
              <p style="color: #666; font-size: 0.875rem; margin: 0.5rem 0;">
                Bio: <%= request.bio %>
              </p>
            <% } %>
            
            <form action="/tags/<%= tag.id %>/manage/request/<%= request.id %>" method="POST">
              <div class="request-actions">
                <div class="response-form">
                  <div class="form-group">
                    <label for="response-<%= request.id %>">Response message (optional):</label>
                    <input type="text" 
                           name="response_message" 
                           id="response-<%= request.id %>"
                           placeholder="Add a message with your decision...">
                  </div>
                </div>
                <div class="action-buttons">
                  <button type="submit" name="action" value="approve" class="btn btn-success">
                    ✅ Approve
                  </button>
                  <button type="submit" name="action" value="reject" class="btn btn-danger">
                    ❌ Reject
                  </button>
                </div>
              </div>
            </form>
          </div>
        <% }) %>
      </div>
    <% } else { %>
      <div class="empty-state">
        <p>No pending access requests</p>
      </div>
    <% } %>
  </div>
  
  <!-- Current Permissions -->
  <div class="section">
    <h2 class="section-title">Current Permissions</h2>
    
    <% if (permissions && permissions.length > 0) { %>
      <div class="permissions-grid">
        <% permissions.forEach(perm => { %>
          <div class="permission-card">
            <% if (perm.user_id) { %>
              <div class="permission-user">@<%= perm.handle || 'Unknown' %></div>
              <div style="color: #666; font-size: 0.875rem;"><%= perm.email %></div>
            <% } else { %>
              <div class="permission-user">All Users</div>
              <div style="color: #666; font-size: 0.875rem;">Universal access</div>
            <% } %>
            <span class="permission-type <%= !perm.user_id ? 'permission-universal' : '' %>">
              <%= perm.permission_type %>
            </span>
          </div>
        <% }) %>
      </div>
    <% } else { %>
      <div class="empty-state">
        <p>No permissions granted yet</p>
      </div>
    <% } %>
  </div>
  
  <!-- Tag Owners -->
  <div class="section">
    <h2 class="section-title">Tag Owners</h2>
    <p style="color: #666; font-size: 0.875rem; margin-bottom: 1rem;">
      <strong>Founder:</strong> Can edit tag details and manage everything<br>
      <strong>Co-owners:</strong> Can manage access requests and permissions
    </p>
    
    <% if (owners && owners.length > 0) { %>
      <div class="owners-list">
        <% owners.forEach(owner => { %>
          <div class="owner-card">
            <div class="owner-name">@<%= owner.handle %></div>
            <div style="color: #666; font-size: 0.875rem;"><%= owner.email %></div>
            <span class="owner-type <%= owner.ownership_type %>">
              <%= owner.ownership_type %>
              <% if (owner.ownership_type === 'founder') { %>
                👑 (Can Edit)
              <% } %>
            </span>
          </div>
        <% }) %>
      </div>
    <% } else { %>
      <div class="empty-state">
        <p>No owners found</p>
      </div>
    <% } %>
  </div>
</div>

<!-- Edit Tag Modal (only for founders) -->
<% if (isFounder) { %>
<div id="editTagModal" class="modal">
  <div class="modal-content">
    <div class="modal-header">
      <h2>✏️ Edit Tag Details</h2>
      <button class="close-modal" onclick="closeEditModal()">&times;</button>
    </div>
    
    <form id="editTagForm" action="/tags/<%= tag.id %>/edit" method="POST" enctype="multipart/form-data">
      <input type="hidden" name="_csrf" value="<%= csrfToken %>" />
      
      <div class="modal-body">
        <div class="form-group">
          <label for="tagName">Tag Name *</label>
          <input 
            type="text" 
            id="tagName" 
            name="name" 
            required 
            value="<%= tag.name %>"
            maxlength="50"
          />
          <div class="char-count" id="nameCount">0 / 50</div>
        </div>
        
        <div class="form-group">
          <label for="shortDescription">Short Description *</label>
          <input 
            type="text" 
            id="shortDescription" 
            name="short_description" 
            required 
            value="<%= tag.short_description || tag.description || '' %>"
            maxlength="100"
          />
          <div class="char-count" id="shortCount">0 / 100</div>
        </div>
        
        <div class="form-group">
          <label for="longDescription">Long Description</label>
          <textarea 
            id="longDescription" 
            name="long_description" 
            maxlength="500"
          ><%= tag.long_description || '' %></textarea>
          <div class="char-count" id="longCount">0 / 500</div>
        </div>
        
        <div class="form-group">
          <label>Tag Image (optional)</label>
          
          <% if (tag.image_url) { %>
            <div class="current-image" style="margin-bottom: 1rem;">
              <p style="margin-bottom: 0.5rem; color: var(--muted, #666);">Current image:</p>
              <img src="<%= tag.image_url %>" alt="Current tag image" style="max-width: 200px; max-height: 200px; border-radius: 8px; display: block; margin-bottom: 0.5rem;">
              <label style="display: inline-flex; align-items: center; gap: 0.5rem; cursor: pointer;">
                <input type="checkbox" name="remove_image" value="true">
                <span style="color: #dc3545;">Remove current image</span>
              </label>
            </div>
          <% } %>
          
          <div class="image-upload-options">
            <div class="upload-tab-buttons">
              <button type="button" class="upload-tab-edit active" data-tab="upload">Upload New Image</button>
              <button type="button" class="upload-tab-edit" data-tab="url">Use URL</button>
            </div>
            
            <div class="upload-tab-content" id="edit-upload-tab">
              <input 
                type="file" 
                id="editImageFile" 
                name="image_file" 
                accept="image/jpeg,image/jpg,image/png,image/gif,image/webp"
                onchange="previewEditImage(this)"
              />
              <div class="help-text">Upload a new image (max 5MB, square images work best)</div>
              <div id="editImagePreview" class="image-preview"></div>
            </div>
            
            <div class="upload-tab-content" id="edit-url-tab" style="display: none;">
              <input 
                type="url" 
                id="editImageUrl" 
                name="image_url" 
                value="<%= tag.image_url && !tag.image_url.startsWith('/static/') ? tag.image_url : '' %>"
                placeholder="https://example.com/image.jpg"
                onchange="previewEditImageUrl(this)"
              />
              <div class="help-text">Enter an image URL (square images work best)</div>
              <div id="editUrlImagePreview" class="image-preview"></div>
            </div>
          </div>
        </div>
        
        <div class="form-group">
          <label>
            <input type="checkbox" name="is_public" value="1" <%= tag.is_public ? 'checked' : '' %> />
            Make this tag public (visible to all users)
          </label>
        </div>
        
        <div class="form-group">
          <label>
            <input type="checkbox" name="auto_approve" value="1" <%= tag.auto_approve ? 'checked' : '' %> />
            ⚡ Enable instant access (auto-approve all requests)
          </label>
          <div class="help-text" style="margin-left: 1.5rem; margin-top: 0.25rem;">
            When enabled, users can get immediate access without waiting for approval - like instant booking on Airbnb
          </div>
        </div>
        
        <div class="info-note">
          <strong>Note:</strong> As the founder, you have exclusive rights to edit this tag. 
          Co-owners can help manage access requests but cannot modify tag details.
        </div>
      </div>
      
      <div class="modal-footer">
        <button type="button" class="btn btn-secondary" onclick="closeEditModal()">Cancel</button>
        <button type="submit" class="btn btn-primary">Save Changes</button>
      </div>
    </form>
  </div>
</div>

<style>
  /* Modal Styles */
  .modal {
    display: none;
    position: fixed;
    z-index: 1000;
    left: 0;
    top: 0;
    width: 100%;
    height: 100%;
    background-color: rgba(0, 0, 0, 0.5);
  }
  
  .modal.active {
    display: flex;
    align-items: center;
    justify-content: center;
  }
  
  .modal-content {
    background-color: var(--card, white);
    color: var(--fg, #333);
    padding: 2rem;
    border-radius: 12px;
    width: 90%;
    max-width: 600px;
    max-height: 90vh;
    overflow-y: auto;
    box-shadow: 0 10px 40px rgba(0, 0, 0, 0.2);
    border: 1px solid var(--border, #e0e0e0);
  }
  
  .modal-header {
    display: flex;
    justify-content: space-between;
    align-items: center;
    margin-bottom: 1.5rem;
    padding-bottom: 1rem;
    border-bottom: 2px solid var(--border, #f0f0f0);
  }
  
  .modal-header h2 {
    margin: 0;
    color: var(--fg, #333);
  }
  
  .close-modal {
    background: none;
    border: none;
    font-size: 1.5rem;
    cursor: pointer;
    color: var(--muted, #999);
    transition: all 0.2s;
    width: 32px;
    height: 32px;
    display: flex;
    align-items: center;
    justify-content: center;
    border-radius: 50%;
  }
  
  .close-modal:hover {
    color: var(--fg, #333);
    background-color: var(--hover-bg, #f0f0f0);
  }
  
  .modal-body {
    margin-bottom: 1.5rem;
  }
  
  .form-group {
    margin-bottom: 1.5rem;
  }
  
  .form-group label {
    display: block;
    margin-bottom: 0.5rem;
    font-weight: 600;
    color: var(--fg, #333);
  }
  
  .form-group input[type="text"],
  .form-group input[type="url"],
  .form-group textarea {
    width: 100%;
    padding: 0.75rem;
    border: 2px solid var(--border, #e0e0e0);
    border-radius: 8px;
    font-size: 1rem;
    transition: border-color 0.2s;
    background-color: var(--bg, white);
    color: var(--fg, #333);
  }
  
  .form-group input:focus,
  .form-group textarea:focus {
    outline: none;
    border-color: var(--primary-color, #6366f1);
    box-shadow: 0 0 0 3px rgba(99, 102, 241, 0.1);
  }
  
  [data-theme="dark"] .form-group input:focus,
  [data-theme="dark"] .form-group textarea:focus {
    box-shadow: 0 0 0 3px rgba(99, 102, 241, 0.2);
  }
  
  .form-group textarea {
    resize: vertical;
    min-height: 80px;
  }
  
  .help-text {
    font-size: 0.875rem;
    color: var(--muted, #666);
    margin-top: 0.25rem;
  }
  
  .char-count {
    text-align: right;
    font-size: 0.875rem;
    color: var(--muted, #999);
    margin-top: 0.25rem;
  }
  
  .char-count.warning {
    color: #ff9800;
  }
  
  .char-count.error {
    color: #f44336;
  }
  
  .modal-footer {
    display: flex;
    justify-content: flex-end;
    gap: 1rem;
    padding-top: 1rem;
    border-top: 2px solid var(--border, #f0f0f0);
  }
  
  .info-note {
    background: var(--hover-bg, #f0f8ff);
    padding: 1rem;
    border-radius: 8px;
    margin-top: 1rem;
    font-size: 0.875rem;
    color: var(--primary-color, #0066cc);
    border: 1px solid var(--border, transparent);
  }
  
  /* Dark mode specific adjustments */
  [data-theme="dark"] .modal {
    background-color: rgba(0, 0, 0, 0.7);
  }
  
  [data-theme="dark"] .modal-content {
    box-shadow: 0 10px 40px rgba(0, 0, 0, 0.5);
  }
  
  /* Image upload styles */
  .image-upload-options {
    margin-top: 0.5rem;
  }
  
  .upload-tab-buttons {
    display: flex;
    gap: 0.5rem;
    margin-bottom: 1rem;
  }
  
  .upload-tab-edit {
    flex: 1;
    padding: 0.5rem 1rem;
    background: var(--hover-bg, #f0f0f0);
    border: 2px solid var(--border, #e0e0e0);
    border-radius: 8px;
    cursor: pointer;
    transition: all 0.2s;
    color: var(--fg, #333);
    font-weight: 500;
  }
  
  .upload-tab-edit:hover {
    background: var(--border, #e0e0e0);
  }
  
  .upload-tab-edit.active {
    background: var(--primary-color, #6366f1);
    color: white;
    border-color: var(--primary-color, #6366f1);
  }
  
  .upload-tab-content {
    padding: 1rem;
    background: var(--hover-bg, #f8f9fa);
    border-radius: 8px;
    border: 1px solid var(--border, #e0e0e0);
  }
  
  input[type="file"] {
    width: 100%;
    padding: 0.75rem;
    border: 2px dashed var(--border, #e0e0e0);
    border-radius: 8px;
    background: var(--bg, white);
    color: var(--fg, #333);
    cursor: pointer;
  }
  
  input[type="file"]:hover {
    border-color: var(--primary-color, #6366f1);
  }
  
  .image-preview {
    margin-top: 1rem;
    text-align: center;
  }
  
  .image-preview img {
    max-width: 200px;
    max-height: 200px;
    border-radius: 8px;
    box-shadow: 0 2px 8px rgba(0, 0, 0, 0.1);
  }
  
  .image-preview .remove-image {
    display: inline-block;
    margin-top: 0.5rem;
    padding: 0.25rem 0.75rem;
    background: #dc3545;
    color: white;
    border: none;
    border-radius: 4px;
    cursor: pointer;
    font-size: 0.875rem;
  }
  
  .image-preview .remove-image:hover {
    background: #c82333;
  }
  
  /* Notification styles */
  .pending-notification {
    background: #fff3cd;
    color: #856404;
    padding: 0.75rem 1rem;
    border-radius: 8px;
    margin-top: 1rem;
    border: 1px solid #ffeaa7;
  }
  
  [data-theme="dark"] .pending-notification {
    background: rgba(255, 193, 7, 0.1);
    color: #ffc107;
    border-color: rgba(255, 193, 7, 0.3);
  }
  
  .pending-badge {
    background: #dc3545;
    color: white;
    padding: 0.25rem 0.5rem;
    border-radius: 12px;
    font-size: 0.875rem;
    margin-left: 0.5rem;
    display: inline-block;
  }
  
  .highlight-border {
    border: 2px solid #ffc107 !important;
    box-shadow: 0 2px 8px rgba(255, 193, 7, 0.1);
  }
  
  [data-theme="dark"] .highlight-border {
    border-color: rgba(255, 193, 7, 0.5) !important;
    box-shadow: 0 2px 8px rgba(255, 193, 7, 0.2);
  }
</style>

<script>
  function showEditTagModal() {
    document.getElementById('editTagModal').classList.add('active');
    updateCharCounts();
  }
  
  function closeEditModal() {
    document.getElementById('editTagModal').classList.remove('active');
  }
  
  // Close modal when clicking outside
  window.addEventListener('click', (e) => {
    const modal = document.getElementById('editTagModal');
    if (e.target === modal) {
      closeEditModal();
    }
  });
  
  // Character count functionality
  function updateCharCount(inputId, countId, maxLength) {
    const input = document.getElementById(inputId);
    const counter = document.getElementById(countId);
    
    if (input && counter) {
      const updateCount = () => {
        const length = input.value.length;
        counter.textContent = `${length} / ${maxLength}`;
        
        // Update color based on length
        counter.classList.remove('warning', 'error');
        if (length > maxLength * 0.9) {
          counter.classList.add('error');
        } else if (length > maxLength * 0.7) {
          counter.classList.add('warning');
        }
      };
      
      input.addEventListener('input', updateCount);
      updateCount();
    }
  }
  
  function updateCharCounts() {
    updateCharCount('tagName', 'nameCount', 50);
    updateCharCount('shortDescription', 'shortCount', 100);
    updateCharCount('longDescription', 'longCount', 500);
  }
  
  // Tab switching for edit modal
  document.addEventListener('DOMContentLoaded', function() {
    document.querySelectorAll('.upload-tab-edit').forEach(tab => {
      tab.addEventListener('click', function() {
        const targetTab = this.dataset.tab;
        
        // Update active tab
        document.querySelectorAll('.upload-tab-edit').forEach(t => t.classList.remove('active'));
        this.classList.add('active');
        
        // Show/hide content
        if (targetTab === 'upload') {
          document.getElementById('edit-upload-tab').style.display = 'block';
          document.getElementById('edit-url-tab').style.display = 'none';
          // Clear URL input when switching to upload
          document.getElementById('editImageUrl').value = '';
          document.getElementById('editUrlImagePreview').innerHTML = '';
        } else {
          document.getElementById('edit-upload-tab').style.display = 'none';
          document.getElementById('edit-url-tab').style.display = 'block';
          // Clear file input when switching to URL
          document.getElementById('editImageFile').value = '';
          document.getElementById('editImagePreview').innerHTML = '';
        }
      });
    });
  });
  
  // Image preview for file upload (edit modal)
  function previewEditImage(input) {
    const preview = document.getElementById('editImagePreview');
    preview.innerHTML = '';
    
    if (input.files && input.files[0]) {
      const file = input.files[0];
      
      // Check file size
      if (file.size > 5 * 1024 * 1024) {
        alert('File size must be less than 5MB');
        input.value = '';
        return;
      }
      
      const reader = new FileReader();
      reader.onload = function(e) {
        preview.innerHTML = `
          <img src="${e.target.result}" alt="Preview">
          <br>
          <button type="button" class="remove-image" onclick="clearEditFileInput()">Remove Image</button>
        `;
      };
      reader.readAsDataURL(file);
    }
  }
  
  // Image preview for URL (edit modal)
  function previewEditImageUrl(input) {
    const preview = document.getElementById('editUrlImagePreview');
    preview.innerHTML = '';
    
    if (input.value) {
      preview.innerHTML = `
        <img src="${input.value}" alt="Preview" onload="this.style.display='block'" onerror="this.style.display='none'; alert('Failed to load image from URL');">
        <br>
        <button type="button" class="remove-image" onclick="clearEditUrlInput()">Remove Image</button>
      `;
    }
  }
  
  // Clear file input (edit modal)
  function clearEditFileInput() {
    document.getElementById('editImageFile').value = '';
    document.getElementById('editImagePreview').innerHTML = '';
  }
  
  // Clear URL input (edit modal)
  function clearEditUrlInput() {
    document.getElementById('editImageUrl').value = '';
    document.getElementById('editUrlImagePreview').innerHTML = '';
  }
</script>
<% } %>
